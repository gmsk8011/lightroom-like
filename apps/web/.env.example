# Enables the AI caption grammar/improve feature.
# Without it the editor works fully; only the Check/Improve buttons are disabled.
# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
